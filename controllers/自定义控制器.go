package controllers

/*
type CustomController struct {
	beego.Controller
}

//获取用户信息
func (c *CustomController) GetUserInfo(){
	beego.Info("获取用户信息")
	username := c.GetString("username")
	userid := c.GetString("userid")
	c.Ctx.Output.Body([]byte("获取用户信息请求，用户名： " + username + ", 用户编号： " + userid))
}

//注册用户信息
func (c *CustomController) RegisterInfo(){
	beego.Info("注册用户信息")
	username := c.GetString("username")
	userid := c.GetString("userid")
	c.Ctx.Output.Body([]byte("用户注册信息，username：" + username + " , userid：" + userid))
}
*/
